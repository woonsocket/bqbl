export class Entry247 {
  /** How many points this is worth. */
  points: number;
  /** In which week the event occurred. */
  week: string;
  /** The name of the scoring team, e.g., "NYJ". */
  team: string;
  /** A short, human-readable description of the event. */
  desc: string;
  /** A URL citation, as proof of the event. */
  url: string;
}
